# Local dev DB (SQLite)
DATABASE_URL="file:./dev.db"

# 32-byte crypto key for encrypting provider credentials (change before production)
CRYPTO_KEY="0123456789abcdef0123456789abcdef"

# JWT secret for admin auth (change before production)
JWT_SECRET="change_this_dev_secret"

# Admin seed account (change after first login)
ADMIN_EMAIL="cuongdzzzzzzz09@gmail.com"
ADMIN_PASSWORD="TempPass!2026"
